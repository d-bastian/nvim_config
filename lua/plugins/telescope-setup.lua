require("telescope").setup({
    defaults = {
        layout_config = {
            prompt_position = "top"
        },
        pickers = {
            find_files = {
                hidden = true
            }
        }
    },
})
